# Paths excluded from the Docker build context.

.dockerignore

# Keep git HEAD + refs so the build can read the current commit hash if needed.
.git
!.git/HEAD
!.git/refs

# Dev/test artifacts
/cover/
/doc/
/test/
/tmp/
.elixir_ls

# Mix artifacts
/_build/
/deps/
*.ez

# Generated on crash by the VM
erl_crash.dump

# Secrets and local databases must never enter the build context
.env
*.db
*.db-*

# Static artifacts — fetched and built inside the image
/assets/node_modules/
/priv/static/assets/
/priv/static/cache_manifest.json
